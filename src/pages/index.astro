---
// src/pages/index.astro
// SnappyX - VIRAL FULLPAGE SCROLL DESIGN 2026
// Full-screen section snapping with smooth animations

const siteTitle = "24/7 Water Damage Restoration Company Serving All 50 States | SnappyX";
const siteDescription = "Emergency water damage restoration services available 24/7/365 across all 50 states. IICRC-certified technicians, rapid 60-minute response, insurance claim assistance.";
const phoneNumber = "(555) 012-3456";
const phoneNumberClean = "5550123456";
const canonicalURL = "https://snappyx.com/";
const currentYear = new Date().getFullYear();

const schemaMarkup = {
  "@context": "https://schema.org",
  "@type": ["LocalBusiness", "EmergencyService"],
  "name": "SnappyX Water Damage Restoration",
  "url": canonicalURL,
  "telephone": phoneNumber,
  "priceRange": "$$",
  "areaServed": { "@type": "Country", "name": "United States" },
  "openingHours": "Mo-Su 00:00-23:59"
};
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{siteTitle}</title>
  <meta name="description" content={siteDescription}>
  <link rel="canonical" href={canonicalURL}>
  
  <meta property="og:title" content={siteTitle}>
  <meta property="og:description" content={siteDescription}>
  <meta property="og:url" content={canonicalURL}>
  
  <script type="application/ld+json" set:html={JSON.stringify(schemaMarkup)} />
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800;900&family=Space+Grotesk:wght@700;800&display=swap" rel="stylesheet">
  
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    html {
      scroll-behavior: smooth;
      overflow-y: scroll;
      scroll-snap-type: y mandatory;
    }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      overflow-x: hidden;
      background: #000;
      color: #fff;
    }
    
    /* FULLPAGE SCROLL SNAP CONTAINER */
    #fullpage-wrapper {
      width: 100%;
      height: 100vh;
      overflow-y: scroll;
      scroll-snap-type: y mandatory;
      scroll-behavior: smooth;
      -webkit-overflow-scrolling: touch;
    }
    
    /* Each Section - Full Screen */
    .fp-section {
      width: 100%;
      height: 100vh;
      scroll-snap-align: start;
      scroll-snap-stop: always;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }
    
    /* ANIMATED GRADIENT TEXT */
    .gradient-text {
      font-family: 'Space Grotesk', sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #667eea 75%, #764ba2 100%);
      background-size: 300% 300%;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: gradient-flow 6s ease infinite;
    }
    
    @keyframes gradient-flow {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }
    
    /* GLASSMORPHISM */
    .glass {
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 20px;
    }
    
    /* FLOATING ANIMATION */
    @keyframes float {
      0%, 100% { transform: translateY(0px) rotate(0deg); }
      33% { transform: translateY(-20px) rotate(2deg); }
      66% { transform: translateY(-10px) rotate(-2deg); }
    }
    
    .float { animation: float 8s ease-in-out infinite; }
    
    /* PULSE GLOW */
    @keyframes pulse {
      0%, 100% { box-shadow: 0 0 20px rgba(239, 68, 68, 0.5); }
      50% { box-shadow: 0 0 60px rgba(239, 68, 68, 0.8), 0 0 100px rgba(239, 68, 68, 0.4); }
    }
    
    .pulse-glow { animation: pulse 2s ease-in-out infinite; }
    
    /* SECTION NAVIGATION DOTS */
    .section-nav {
      position: fixed;
      right: 30px;
      top: 50%;
      transform: translateY(-50%);
      z-index: 1000;
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    
    .section-nav-dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      cursor: pointer;
      transition: all 0.3s ease;
      border: 2px solid transparent;
    }
    
    .section-nav-dot:hover {
      background: rgba(255, 255, 255, 0.6);
      transform: scale(1.3);
    }
    
    .section-nav-dot.active {
      background: #3b82f6;
      border-color: #fff;
      transform: scale(1.4);
    }
    
    /* SCROLL INDICATOR */
    .scroll-indicator {
      position: absolute;
      bottom: 40px;
      left: 50%;
      transform: translateX(-50%);
      animation: bounce 2s infinite;
    }
    
    @keyframes bounce {
      0%, 20%, 50%, 80%, 100% { transform: translateX(-50%) translateY(0); }
      40% { transform: translateX(-50%) translateY(-15px); }
      60% { transform: translateX(-50%) translateY(-7px); }
    }
    
    /* MESH GRADIENT BACKGROUNDS */
    .mesh-1 {
      background: radial-gradient(at 0% 0%, hsla(253,100%,67%,1) 0px, transparent 50%),
                  radial-gradient(at 50% 0%, hsla(225,39%,67%,1) 0px, transparent 50%),
                  radial-gradient(at 100% 0%, hsla(339,100%,67%,1) 0px, transparent 50%);
    }
    
    .mesh-2 {
      background: radial-gradient(at 0% 50%, hsla(217,100%,50%,1) 0px, transparent 50%),
                  radial-gradient(at 50% 50%, hsla(186,100%,69%,1) 0px, transparent 50%),
                  radial-gradient(at 100% 50%, hsla(355,100%,93%,1) 0px, transparent 50%);
    }
    
    .mesh-3 {
      background: radial-gradient(at 0% 100%, hsla(120,100%,50%,0.3) 0px, transparent 50%),
                  radial-gradient(at 50% 100%, hsla(39,100%,50%,0.3) 0px, transparent 50%),
                  radial-gradient(at 100% 100%, hsla(242,100%,70%,0.3) 0px, transparent 50%);
    }
    
    /* CARD HOVER EFFECTS */
    .hover-card {
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
    }
    
    .hover-card:hover {
      transform: translateY(-15px) scale(1.05);
      box-shadow: 0 30px 60px rgba(0, 0, 0, 0.3);
    }
    
    /* BUTTON EFFECTS */
    .btn-primary {
      position: relative;
      overflow: hidden;
      transition: all 0.3s ease;
    }
    
    .btn-primary::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 0;
      height: 0;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.3);
      transform: translate(-50%, -50%);
      transition: width 0.6s, height 0.6s;
    }
    
    .btn-primary:hover::before {
      width: 300px;
      height: 300px;
    }
    
    /* RESPONSIVE */
    @media (max-width: 768px) {
      .section-nav { right: 15px; }
      .fp-section { height: 100svh; } /* Mobile viewport height */
    }
    
    /* HIDE SCROLLBAR */
    #fullpage-wrapper::-webkit-scrollbar { width: 0; height: 0; }
    
    /* TEXT REVEAL ANIMATION */
    @keyframes text-reveal {
      from {
        opacity: 0;
        transform: translateY(50px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .reveal-text {
      animation: text-reveal 1s ease-out forwards;
    }
  </style>
</head>

<body>

<!-- SECTION NAVIGATION DOTS -->
<div class="section-nav">
  <div class="section-nav-dot active" data-section="0"></div>
  <div class="section-nav-dot" data-section="1"></div>
  <div class="section-nav-dot" data-section="2"></div>
  <div class="section-nav-dot" data-section="3"></div>
  <div class="section-nav-dot" data-section="4"></div>
  <div class="section-nav-dot" data-section="5"></div>
  <div class="section-nav-dot" data-section="6"></div>
</div>

<!-- FULLPAGE SCROLL WRAPPER -->
<div id="fullpage-wrapper">

  <!-- ==================== SECTION 1: HERO ==================== -->
  <section class="fp-section mesh-1" id="section-0">
    <div class="container mx-auto px-6 text-center relative z-10">
      
      <!-- Floating Logo -->
      <div class="float mb-8">
        <h1 class="text-6xl md:text-8xl font-black mb-6">
          <span class="gradient-text">SnappyX</span>
        </h1>
      </div>
      
      <!-- Main Headline -->
      <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold leading-tight mb-6 reveal-text" style="animation-delay: 0.2s;">
        24/7 Water Damage<br/>
        <span class="gradient-text">Restoration Company</span><br/>
        Serving All 50 States
      </h1>
      
      <!-- Subtext -->
      <p class="text-xl md:text-2xl text-gray-300 max-w-3xl mx-auto mb-10 reveal-text" style="animation-delay: 0.4s;">
        Emergency response in <strong class="text-blue-400">60 minutes</strong> or less. IICRC-certified specialists ready nationwide.
      </p>
      
      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-6 justify-center items-center reveal-text" style="animation-delay: 0.6s;">
        <a href={`tel:+1${phoneNumberClean}`} 
           class="btn-primary glass px-10 py-5 text-xl font-bold rounded-full pulse-glow hover:scale-110 transition-all duration-300 relative z-10">
          🚨 CALL NOW: {phoneNumber}
        </a>
        <a href="#section-2" 
           class="glass px-10 py-5 text-xl font-semibold rounded-full hover:bg-white/10 transition-all duration-300">
          Our Services →
        </a>
      </div>
      
      <!-- Trust Badges -->
      <div class="mt-16 flex flex-wrap justify-center gap-6 reveal-text" style="animation-delay: 0.8s;">
        <div class="glass px-6 py-3 rounded-full text-sm font-semibold">✅ IICRC Certified</div>
        <div class="glass px-6 py-3 rounded-full text-sm font-semibold">⚡ 60-Min Response</div>
        <div class="glass px-6 py-3 rounded-full text-sm font-semibold">🛡️ Licensed & Insured</div>
        <div class="glass px-6 py-3 rounded-full text-sm font-semibold">🇺🇸 All 50 States</div>
      </div>
      
      <!-- Scroll Indicator -->
      <div class="scroll-indicator">
        <svg class="w-8 h-8 text-white opacity-50" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </div>
    </div>
  </section>

  <!-- ==================== SECTION 2: STATS ==================== -->
  <section class="fp-section bg-gradient-to-br from-blue-900 via-blue-800 to-purple-900" id="section-1">
    <div class="container mx-auto px-6 text-center">
      
      <h2 class="text-5xl md:text-6xl font-bold mb-16 gradient-text">Trusted Nationwide</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-12 max-w-5xl mx-auto">
        
        <!-- Stat 1 -->
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="text-6xl md:text-7xl font-black mb-4 counter" data-target="15">0</div>
          <div class="text-2xl text-gray-300 font-medium">Years in Business</div>
        </div>
        
        <!-- Stat 2 -->
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="text-6xl md:text-7xl font-black mb-4 counter" data-target="50000">0</div>
          <div class="text-2xl text-gray-300 font-medium">Jobs Completed</div>
        </div>
        
        <!-- Stat 3 -->
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="text-6xl md:text-7xl font-black mb-4"><span class="counter" data-target="60">0</span> Min</div>
          <div class="text-2xl text-gray-300 font-medium">Avg Response Time</div>
        </div>
        
      </div>
      
      <!-- Bottom CTA -->
      <div class="mt-16">
        <p class="text-2xl text-gray-300 mb-6">Water damage doesn't wait. Neither do we.</p>
        <a href={`tel:+1${phoneNumberClean}`} 
           class="inline-block glass px-12 py-6 text-2xl font-bold rounded-full pulse-glow hover:scale-110 transition-all duration-300">
          Get Emergency Help Now
        </a>
      </div>
    </div>
  </section>

  <!-- ==================== SECTION 3: SERVICES ==================== -->
  <section class="fp-section mesh-2" id="section-2">
    <div class="container mx-auto px-6">
      
      <div class="text-center mb-12">
        <h2 class="text-5xl md:text-6xl font-bold mb-6">
          <span class="gradient-text">Emergency Services</span>
        </h2>
        <p class="text-xl text-gray-300 max-w-2xl mx-auto">Comprehensive water damage solutions delivered by certified professionals</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        
        <!-- Service 1 -->
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="w-20 h-20 bg-blue-500/30 rounded-2xl flex items-center justify-center mb-6 mx-auto">
            <svg class="w-10 h-10 text-blue-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4">Water Damage Restoration</h3>
          <p class="text-gray-300 leading-relaxed">Complete property restoration from assessment through final repairs using IICRC-certified protocols.</p>
        </div>

        <!-- Service 2 -->
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="w-20 h-20 bg-red-500/30 rounded-2xl flex items-center justify-center mb-6 mx-auto">
            <svg class="w-10 h-10 text-red-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4">Emergency Water Removal</h3>
          <p class="text-gray-300 leading-relaxed">Immediate 24/7 water extraction with commercial-grade pumps and rapid-deployment teams.</p>
        </div>

        <!-- Service 3 -->
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="w-20 h-20 bg-cyan-500/30 rounded-2xl flex items-center justify-center mb-6 mx-auto">
            <svg class="w-10 h-10 text-cyan-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4">Flood Cleanup</h3>
          <p class="text-gray-300 leading-relaxed">Specialized flood remediation for natural disasters with FEMA coordination.</p>
        </div>

        <!-- Service 4 -->
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="w-20 h-20 bg-indigo-500/30 rounded-2xl flex items-center justify-center mb-6 mx-auto">
            <svg class="w-10 h-10 text-indigo-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4">Structural Drying</h3>
          <p class="text-gray-300 leading-relaxed">Advanced moisture detection using thermal imaging and precision monitoring.</p>
        </div>

        <!-- Service 5 -->
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="w-20 h-20 bg-green-500/30 rounded-2xl flex items-center justify-center mb-6 mx-auto">
            <svg class="w-10 h-10 text-green-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
            </svg>
          </div>
          <h3 class="text-2xl font-bold mb-4">Mold Mitigation</h3>
          <p class="text-gray-300 leading-relaxed">Certified mold remediation following IICRC S520 standards with HEPA filtration.</p>
        </div>
        
      </div>
    </div>
  </section>

  <!-- ==================== SECTION 4: WHY CHOOSE US ==================== -->
  <section class="fp-section bg-gradient-to-br from-purple-900 via-pink-900 to-red-900" id="section-3">
    <div class="container mx-auto px-6">
      
      <div class="text-center mb-12">
        <h2 class="text-5xl md:text-6xl font-bold mb-6">
          <span class="gradient-text">Why Choose SnappyX</span>
        </h2>
        <p class="text-xl text-gray-300">Trusted nationwide for emergency restoration</p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-5xl mx-auto">
        
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="flex items-start gap-4">
            <svg class="w-8 h-8 text-green-400 flex-shrink-0 mt-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <div>
              <h3 class="text-2xl font-bold mb-3">Licensed & Insured</h3>
              <p class="text-gray-300 leading-relaxed">Fully licensed contractors with comprehensive liability and workers' comp coverage in all 50 states.</p>
            </div>
          </div>
        </div>
        
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="flex items-start gap-4">
            <svg class="w-8 h-8 text-blue-400 flex-shrink-0 mt-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <div>
              <h3 class="text-2xl font-bold mb-3">IICRC-Certified</h3>
              <p class="text-gray-300 leading-relaxed">Every technician maintains current Water Restoration Technician (WRT) certification with ongoing training.</p>
            </div>
          </div>
        </div>
        
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="flex items-start gap-4">
            <svg class="w-8 h-8 text-yellow-400 flex-shrink-0 mt-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <div>
              <h3 class="text-2xl font-bold mb-3">Insurance Assistance</h3>
              <p class="text-gray-300 leading-relaxed">Direct billing with major carriers, detailed documentation, and preferred vendor relationships.</p>
            </div>
          </div>
        </div>
        
        <div class="glass p-8 rounded-3xl hover-card">
          <div class="flex items-start gap-4">
            <svg class="w-8 h-8 text-red-400 flex-shrink-0 mt-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <div>
              <h3 class="text-2xl font-bold mb-3">24/7/365 Response</h3>
              <p class="text-gray-300 leading-relaxed">Round-the-clock emergency dispatch with immediate deployment regardless of time or weather.</p>
            </div>
          </div>
        </div>
        
      </div>
    </div>
  </section>

  <!-- ==================== SECTION 5: NATIONWIDE COVERAGE ==================== -->
  <section class="fp-section mesh-3" id="section-4">
    <div class="container mx-auto px-6">
      
      <div class="text-center mb-12">
        <h2 class="text-5xl md:text-6xl font-bold mb-6">
          <span class="gradient-text">Nationwide Coverage</span>
        </h2>
        <p class="text-xl text-gray-300">Professional restoration services in all 50 United States</p>
      </div>
      
      <div class="max-w-5xl mx-auto grid grid-cols-2 md:grid-cols-5 gap-4">
        <a href="/texas" class="glass p-4 rounded-xl text-center hover-card font-semibold">Texas</a>
        <a href="/florida" class="glass p-4 rounded-xl text-center hover-card font-semibold">Florida</a>
        <a href="/california" class="glass p-4 rounded-xl text-center hover-card font-semibold">California</a>
        <a href="/new-york" class="glass p-4 rounded-xl text-center hover-card font-semibold">New York</a>
        <a href="/ohio" class="glass p-4 rounded-xl text-center hover-card font-semibold">Ohio</a>
        <a href="/illinois" class="glass p-4 rounded-xl text-center hover-card font-semibold">Illinois</a>
        <a href="/pennsylvania" class="glass p-4 rounded-xl text-center hover-card font-semibold">Pennsylvania</a>
        <a href="/georgia" class="glass p-4 rounded-xl text-center hover-card font-semibold">Georgia</a>
        <a href="/north-carolina" class="glass p-4 rounded-xl text-center hover-card font-semibold">N. Carolina</a>
        <a href="/virginia" class="glass p-4 rounded-xl text-center hover-card font-semibold">Virginia</a>
        <a href="/washington" class="glass p-4 rounded-xl text-center hover-card font-semibold">Washington</a>
        <a href="/massachusetts" class="glass p-4 rounded-xl text-center hover-card font-semibold">Massachusetts</a>
        <a href="/arizona" class="glass p-4 rounded-xl text-center hover-card font-semibold">Arizona</a>
        <a href="/tennessee" class="glass p-4 rounded-xl text-center hover-card font-semibold">Tennessee</a>
        <a href="/colorado" class="glass p-4 rounded-xl text-center hover-card font-semibold">Colorado</a>
      </div>
      
      <div class="text-center mt-10">
        <a href="/states" class="glass px-10 py-4 rounded-full font-bold hover:bg-white/10 transition-all inline-block">
          View All 50 States →
        </a>
      </div>
    </div>
  </section>

  <!-- ==================== SECTION 6: EMERGENCY CTA ==================== -->
  <section class="fp-section bg-gradient-to-br from-red-900 via-red-800 to-orange-900" id="section-5">
    <div class="container mx-auto px-6 text-center">
      
      <div class="max-w-4xl mx-auto">
        <div class="text-7xl mb-8">🚨</div>
        
        <h2 class="text-5xl md:text-7xl font-black mb-8 leading-tight">
          Water Emergency?<br/>
          <span class="gradient-text">We're Here RIGHT NOW</span>
        </h2>
        
        <p class="text-2xl md:text-3xl text-gray-200 mb-12">
          Call our 24/7 emergency hotline for immediate assistance
        </p>
        
        <a href={`tel:+1${phoneNumberClean}`} 
           class="inline-block glass px-16 py-8 text-4xl md:text-5xl font-black rounded-full pulse-glow hover:scale-110 transition-all duration-300">
          {phoneNumber}
        </a>
        
        <p class="text-lg text-gray-300 mt-8">Certified teams standing by nationwide</p>
      </div>
    </div>
  </section>

  <!-- ==================== SECTION 7: FOOTER ==================== -->
  <section class="fp-section bg-gray-900" id="section-6">
    <div class="container mx-auto px-6">
      
      <div class="max-w-6xl mx-auto">
        
        <div class="text-center mb-12">
          <div class="text-5xl font-black mb-6">
            <span class="text-blue-400">Snappy</span><span class="text-red-400">X</span>
          </div>
          <p class="text-xl text-gray-400 max-w-2xl mx-auto">
            Professional water damage restoration and emergency services available 24/7 across all 50 United States.
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
          
          <div>
            <h3 class="text-xl font-bold mb-4 text-white">Quick Links</h3>
            <ul class="space-y-2 text-gray-400">
              <li><a href="/" class="hover:text-blue-400 transition-colors">Home</a></li>
              <li><a href="/about" class="hover:text-blue-400 transition-colors">About Us</a></li>
              <li><a href="/services" class="hover:text-blue-400 transition-colors">Services</a></li>
              <li><a href="/states" class="hover:text-blue-400 transition-colors">Service Areas</a></li>
              <li><a href="/contact" class="hover:text-blue-400 transition-colors">Contact</a></li>
            </ul>
          </div>
          
          <div>
            <h3 class="text-xl font-bold mb-4 text-white">Legal</h3>
            <ul class="space-y-2 text-gray-400">
              <li><a href="/privacy" class="hover:text-blue-400 transition-colors">Privacy Policy</a></li>
              <li><a href="/terms" class="hover:text-blue-400 transition-colors">Terms of Service</a></li>
              <li><a href="/licenses" class="hover:text-blue-400 transition-colors">Licenses</a></li>
            </ul>
          </div>
          
          <div>
            <h3 class="text-xl font-bold mb-4 text-white">Emergency Contact</h3>
            <a href={`tel:+1${phoneNumberClean}`} class="text-3xl font-bold text-blue-400 hover:text-blue-300">
              {phoneNumber}
            </a>
            <p class="text-gray-400 mt-2">Available 24/7/365</p>
          </div>
          
        </div>
        
        <div class="border-t border-gray-800 pt-8 text-center text-gray-500">
          <p>&copy; {currentYear} SnappyX Water Damage Restoration. All rights reserved.</p>
          <p class="text-sm mt-2">Licensed, Insured & IICRC-Certified in All 50 States</p>
        </div>
        
      </div>
    </div>
  </section>

</div>

<!-- JAVASCRIPT FOR SCROLL SNAP & INTERACTIONS -->
<script>
  // Section Navigation Dots
  const dots = document.querySelectorAll('.section-nav-dot');
  const sections = document.querySelectorAll('.fp-section');
  const wrapper = document.getElementById('fullpage-wrapper');
  
  // Update active dot on scroll
  wrapper.addEventListener('scroll', () => {
    let current = 0;
    sections.forEach((section, index) => {
      const rect = section.getBoundingClientRect();
      if (rect.top >= -100 && rect.top <= 100) {
        current = index;
      }
    });
    
    dots.forEach((dot, index) => {
      dot.classList.toggle('active', index === current);
    });
  });
  
  // Click navigation
  dots.forEach((dot, index) => {
    dot.addEventListener('click', () => {
      sections[index].scrollIntoView({ behavior: 'smooth' });
    });
  });
  
  // Keyboard Navigation (Arrow Keys)
  let currentSection = 0;
  document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowDown' && currentSection < sections.length - 1) {
      currentSection++;
      sections[currentSection].scrollIntoView({ behavior: 'smooth' });
    } else if (e.key === 'ArrowUp' && currentSection > 0) {
      currentSection--;
      sections[currentSection].scrollIntoView({ behavior: 'smooth' });
    }
  });
  
  // Animated Counters
  function animateCounter(element) {
    const target = parseInt(element.getAttribute('data-target'));
    const duration = 2000;
    const increment = target / (duration / 16);
    let current = 0;
    
    const timer = setInterval(() => {
      current += increment;
      if (current >= target) {
        element.textContent = target.toLocaleString();
        clearInterval(timer);
      } else {
        element.textContent = Math.floor(current).toLocaleString();
      }
    }, 16);
  }
  
  // Trigger counters on section view
  const counters = document.querySelectorAll('.counter');
  const counterObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        animateCounter(entry.target);
        counterObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.5 });
  
  counters.forEach(counter => counterObserver.observe(counter));
</script>

</body>
</html>
