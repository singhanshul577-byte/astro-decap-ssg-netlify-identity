---
// Homepage - Emergency Water Damage Restoration
const states = [
  { name: "Ohio", slug: "ohio", emoji: "🏛️", cities: 50 },
  { name: "New York", slug: "new-york", emoji: "🗽", cities: 60 },
  { name: "Florida", slug: "florida", emoji: "🌴", cities: 40 },
  { name: "California", slug: "california", emoji: "☀️", cities: 80 },
  { name: "Texas", slug: "texas", emoji: "🤠", cities: 40 },
  { name: "Michigan", slug: "michigan", emoji: "🚗", cities: 45 },
  { name: "Illinois", slug: "illinois", emoji: "🌃", cities: 35 },
  { name: "Massachusetts", slug: "massachusetts", emoji: "🎓", cities: 50 },
  { name: "New Jersey", slug: "new-jersey", emoji: "🏖️", cities: 60 },
  { name: "Connecticut", slug: "connecticut", emoji: "🏛️", cities: 40 }
];

const popularAreas = [
  { name: "Scarsdale, NY", slug: "scarsdale-ny", time: "2 hrs ago" },
  { name: "Chagrin Falls, OH", slug: "chagrin-falls-oh", time: "47 min ago" },
  { name: "Zionsville, IN", slug: "zionsville-in", time: "1 hr ago" },
  { name: "Birmingham, MI", slug: "birmingham-mi", time: "3 hrs ago" },
  { name: "Pinecrest, FL", slug: "pinecrest-fl", time: "5 hrs ago" },
  { name: "Orinda, CA", slug: "orinda-ca", time: "45 min ago" },
  { name: "Wellesley, MA", slug: "wellesley-ma", time: "2 hrs ago" },
  { name: "Hinsdale, IL", slug: "hinsdale-il", time: "4 hrs ago" },
  { name: "Greenwich, CT", slug: "greenwich-ct", time: "1 hr ago" },
  { name: "McLean, VA", slug: "mclean-va", time: "3 hrs ago" },
  { name: "Los Altos, CA", slug: "los-altos-ca", time: "6 hrs ago" },
  { name: "Highland Park, IL", slug: "highland-park-il", time: "2 hrs ago" }
];
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>🚨 Emergency Water Damage Restoration | 60 Min Response - 24/7 Service</title>
  <meta name="description" content="Emergency water damage restoration. 60-minute response to 500+ US communities. Available 24/7. $0 out of pocket with insurance. Call now for immediate help!">
  
  <!-- Schema Markup -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "EmergencyService",
    "name": "SnappyX Emergency Water Restoration",
    "telephone": "+1-555-012-3456",
    "url": "https://snappyx.pro",
    "areaServed": {
      "@type": "Country",
      "name": "United States"
    },
    "availableChannel": {
      "@type": "ServiceChannel",
      "serviceType": "Emergency Water Damage Restoration",
      "availableLanguage": "English"
    },
    "hoursAvailable": "24/7"
  }
  </script>
</head>
<body>

  <!-- Header -->
  <header class="header">
    <nav class="nav">
      <div class="logo">
        <span class="logo-icon">⚡</span>
        <span class="logo-text">SnappyX</span>
      </div>
      <a href="tel:+15550123456" class="cta-btn pulse">
        📞 CALL NOW
      </a>
    </nav>
  </header>

  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-content">
      <div class="hero-badge fade-in">🚨 EMERGENCY SERVICE</div>
      <h1 class="hero-title slide-up">FLOODED BASEMENT?</h1>
      <p class="hero-subtitle slide-up-delay">We arrive in 60 Minutes or Less. 24/7 Emergency Service.</p>
      
      <a href="tel:+15550123456" class="hero-cta bounce">
        <span class="cta-icon">📞</span>
        Call Now: (555) 012-3456
      </a>
      
      <div class="live-status fade-in-delay">
        <span class="pulse-dot"></span>
        Live Technicians Available Right Now
      </div>
      
      <p class="coverage fade-in-delay">Serving 500+ US Communities Nationwide</p>
    </div>
    <div class="hero-pattern"></div>
  </section>

  <!-- Trust Bar -->
  <section class="trust-bar">
    <div class="trust-bar-content">
      <div class="trust-item">
        <span class="trust-icon">⭐</span>
        <span class="trust-text"><strong>4.9/5</strong> Rating</span>
      </div>
      <div class="trust-divider"></div>
      <div class="trust-item">
        <span class="trust-icon">📞</span>
        <span class="trust-text"><strong>2,847</strong> Calls Today</span>
      </div>
      <div class="trust-divider"></div>
      <div class="trust-item">
        <span class="trust-icon">⏱️</span>
        <span class="trust-text"><strong>47 Min</strong> Avg Response</span>
      </div>
    </div>
  </section>

  <!-- State Selector -->
  <section class="states-section">
    <div class="container">
      <h2 class="section-title">Select Your State for Immediate Service</h2>
      <p class="section-subtitle">Emergency teams standing by in your area</p>
      
      <div class="states-grid">
        {states.map(state => (
          <a href={`/${state.slug}`} class="state-card">
            <div class="state-emoji">{state.emoji}</div>
            <h3 class="state-name">{state.name}</h3>
            <p class="state-cities">{state.cities} Cities</p>
            <div class="state-badge">24/7 Service</div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section class="services-section">
    <div class="container">
      <h2 class="section-title">Our Emergency Services</h2>
      
      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon">💧</div>
          <h3>Water Extraction</h3>
          <p>Powerful pumps remove standing water from basements and carpets immediately</p>
        </div>
        
        <div class="service-card">
          <div class="service-icon">❄️</div>
          <h3>Frozen/Burst Pipes</h3>
          <p>We fix the leak and dry the damage caused by winter pipe bursts</p>
        </div>
        
        <div class="service-card">
          <div class="service-icon">💩</div>
          <h3>Sewage Cleanup</h3>
          <p>Safe removal of hazardous sewage backup. We sanitize and deodorize</p>
        </div>
        
        <div class="service-card">
          <div class="service-icon">🦠</div>
          <h3>Mold Prevention</h3>
          <p>Prevent toxic mold growth with industrial drying and dehumidification</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Popular Areas -->
  <section class="popular-section">
    <div class="container">
      <h2 class="section-title">Recently Served Emergency Areas</h2>
      <p class="section-subtitle">Live dispatch activity - your area is covered!</p>
      
      <div class="popular-grid">
        {popularAreas.map(area => (
          <a href={`/${area.slug}`} class="popular-card">
            <div class="popular-indicator">
              <span class="indicator-dot"></span>
              <span class="indicator-text">{area.time}</span>
            </div>
            <h3 class="popular-name">{area.name}</h3>
            <span class="popular-arrow">→</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Trust Section -->
  <section class="trust-section">
    <div class="container">
      <h2 class="section-title">Why Homeowners Trust Us</h2>
      
      <div class="trust-grid">
        <div class="trust-point">
          <div class="trust-check">✅</div>
          <h3>$0 Out of Pocket</h3>
          <p>We bill your Insurance Company directly. Most homeowners pay nothing.</p>
        </div>
        
        <div class="trust-point">
          <div class="trust-check">✅</div>
          <h3>Licensed & Insured</h3>
          <p>Fully certified IICRC technicians. All work guaranteed.</p>
        </div>
        
        <div class="trust-point">
          <div class="trust-check">✅</div>
          <h3>30-60 Min Response</h3>
          <p>Because water damage gets worse every minute. We arrive fast.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="final-cta">
    <div class="container">
      <div class="cta-content">
        <h2>🚨 Emergency? Don't Wait!</h2>
        <p>Every minute increases damage and costs. Call now for immediate dispatch.</p>
        <a href="tel:+15550123456" class="hero-cta pulse">
          <span class="cta-icon">📞</span>
          Call Now: (555) 012-3456
        </a>
        <p class="cta-small">Available 24/7 • 60-Minute Response • $0 With Insurance</p>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-brand">
          <h3>⚡ SnappyX Restoration</h3>
          <p>24/7 Emergency Water Damage Response</p>
        </div>
        
        <div class="footer-links">
          <h4>Coverage Areas</h4>
          <div class="footer-states">
            {states.map(state => (
              <a href={`/${state.slug}`}>{state.name}</a>
            ))}
          </div>
        </div>
      </div>
      
      <div class="footer-bottom">
        <p>© 2025 SnappyX Restoration Services. Licensed & Insured. All Rights Reserved.</p>
        <p class="footer-emergency">🚨 Emergency Hotline: <a href="tel:+15550123456">(555) 012-3456</a></p>
      </div>
    </div>
  </footer>

</body>
</html>

<style>
  /* Reset & Base */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
    line-height: 1.6;
    color: #1f2937;
    overflow-x: hidden;
  }
  
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }
  
  /* Header */
  .header {
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(10px);
    padding: 1rem 0;
    box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
    position: sticky;
    top: 0;
    z-index: 1000;
    animation: slideDown 0.5s ease-out;
  }
  
  .nav {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.5rem;
    font-weight: 800;
    color: #1e40af;
  }
  
  .logo-icon {
    font-size: 2rem;
    animation: pulse 2s infinite;
  }
  
  .cta-btn {
    background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
    color: white;
    padding: 0.875rem 2rem;
    border-radius: 50px;
    font-weight: 700;
    text-decoration: none;
    box-shadow: 0 4px 15px rgba(220, 38, 38, 0.3);
    transition: all 0.3s ease;
    font-size: 1.1rem;
  }
  
  .cta-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 25px rgba(220, 38, 38, 0.4);
  }
  
  /* Hero */
  .hero {
    background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 50%, #3b82f6 100%);
    color: white;
    padding: 5rem 1.5rem;
    text-align: center;
    position: relative;
    overflow: hidden;
    min-height: 600px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .hero-pattern {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: 
      radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.3) 0%, transparent 50%),
      radial-gradient(circle at 80% 80%, rgba(147, 197, 253, 0.3) 0%, transparent 50%);
    pointer-events: none;
  }
  
  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 900px;
  }
  
  .hero-badge {
    display: inline-block;
    background: rgba(220, 38, 38, 0.9);
    padding: 0.5rem 1.5rem;
    border-radius: 50px;
    font-weight: 700;
    font-size: 0.875rem;
    letter-spacing: 1px;
    margin-bottom: 1.5rem;
  }
  
  .hero-title {
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    font-weight: 900;
    margin-bottom: 1rem;
    text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    line-height: 1.1;
  }
  
  .hero-subtitle {
    font-size: clamp(1.25rem, 4vw, 1.75rem);
    margin-bottom: 2.5rem;
    opacity: 0.95;
    font-weight: 500;
  }
  
  .hero-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    background: linear-gradient(135deg, #dc2626 0%, #b91c1c 100%);
    color: white;
    padding: 1.5rem 3rem;
    border-radius: 60px;
    font-size: 1.75rem;
    font-weight: 800;
    text-decoration: none;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
    transition: all 0.3s ease;
    margin-bottom: 2rem;
  }
  
  .hero-cta:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 15px 50px rgba(0, 0, 0, 0.4);
  }
  
  .cta-icon {
    font-size: 2rem;
  }
  
  .live-status {
    background: rgba(16, 185, 129, 0.2);
    backdrop-filter: blur(10px);
    padding: 1rem 2rem;
    border-radius: 50px;
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
    border: 2px solid rgba(16, 185, 129, 0.3);
  }
  
  .pulse-dot {
    width: 12px;
    height: 12px;
    background: #10b981;
    border-radius: 50%;
    animation: pulse-dot 2s infinite;
  }
  
  .coverage {
    font-size: 1.25rem;
    opacity: 0.9;
  }
  
  /* Trust Bar */
  .trust-bar {
    background: linear-gradient(135deg, #f3f4f6 0%, #e5e7eb 100%);
    padding: 1.5rem;
    border-bottom: 1px solid #d1d5db;
  }
  
  .trust-bar-content {
    max-width: 1000px;
    margin: 0 auto;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 2rem;
    flex-wrap: wrap;
  }
  
  .trust-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1rem;
  }
  
  .trust-icon {
    font-size: 1.5rem;
  }
  
  .trust-divider {
    width: 1px;
    height: 30px;
    background: #9ca3af;
  }
  
  /* State Selector */
  .states-section {
    padding: 5rem 0;
    background: white;
  }
  
  .section-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    text-align: center;
    color: #1e40af;
    margin-bottom: 1rem;
  }
  
  .section-subtitle {
    text-align: center;
    font-size: 1.25rem;
    color: #6b7280;
    margin-bottom: 3rem;
  }
  
  .states-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    margin-top: 3rem;
  }
  
  .state-card {
    background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%);
    padding: 2rem;
    border-radius: 20px;
    text-align: center;
    text-decoration: none;
    color: inherit;
    border: 2px solid #e5e7eb;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }
  
  .state-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, #3b82f6 0%, #1e40af 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
    z-index: 0;
  }
  
  .state-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(30, 64, 175, 0.2);
    border-color: #3b82f6;
  }
  
  .state-card:hover::before {
    opacity: 1;
  }
  
  .state-card:hover * {
    color: white !important;
  }
  
  .state-emoji {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    position: relative;
    z-index: 1;
  }
  
  .state-name {
    font-size: 1.5rem;
    font-weight: 700;
    color: #1f2937;
    margin-bottom: 0.5rem;
    position: relative;
    z-index: 1;
  }
  
  .state-cities {
    color: #6b7280;
    font-size: 1rem;
    margin-bottom: 1rem;
    position: relative;
    z-index: 1;
  }
  
  .state-badge {
    display: inline-block;
    background: #10b981;
    color: white;
    padding: 0.375rem 1rem;
    border-radius: 50px;
    font-size: 0.875rem;
    font-weight: 600;
    position: relative;
    z-index: 1;
  }
  
  /* Services */
  .services-section {
    padding: 5rem 0;
    background: linear-gradient(135deg, #f9fafb 0%, #f3f4f6 100%);
  }
  
  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }
  
  .service-card {
    background: white;
    padding: 2.5rem;
    border-radius: 20px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    text-align: center;
    transition: all 0.3s ease;
    border: 2px solid transparent;
  }
  
  .service-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
    border-color: #3b82f6;
  }
  
  .service-icon {
    font-size: 4rem;
    margin-bottom: 1.5rem;
    animation: float 3s ease-in-out infinite;
  }
  
  .service-card h3 {
    font-size: 1.5rem;
    color: #1e40af;
    margin-bottom: 1rem;
    font-weight: 700;
  }
  
  .service-card p {
    color: #6b7280;
    line-height: 1.7;
  }
  
  /* Popular Areas */
  .popular-section {
    padding: 5rem 0;
    background: white;
  }
  
  .popular-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-top: 3rem;
  }
  
  .popular-card {
    background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
    padding: 1.5rem;
    border-radius: 15px;
    text-decoration: none;
    color: inherit;
    border: 2px solid #bfdbfe;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }
  
  .popular-card:hover {
    transform: translateX(8px);
    box-shadow: 0 10px 30px rgba(59, 130, 246, 0.2);
    border-color: #3b82f6;
  }
  
  .popular-indicator {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: #10b981;
    font-weight: 600;
  }
  
  .indicator-dot {
    width: 8px;
    height: 8px;
    background: #10b981;
    border-radius: 50%;
    animation: pulse-dot 2s infinite;
  }
  
  .popular-name {
    font-size: 1.25rem;
    font-weight: 700;
    color: #1e40af;
    flex: 1;
  }
  
  .popular-arrow {
    font-size: 1.5rem;
    color: #3b82f6;
  }
  
  /* Trust Section */
  .trust-section {
    padding: 5rem 0;
    background: linear-gradient(135deg, #dbeafe 0%, #bfdbfe 100%);
  }
  
  .trust-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2.5rem;
    margin-top: 3rem;
  }
  
  .trust-point {
    text-align: center;
  }
  
  .trust-check {
    font-size: 4rem;
    margin-bottom: 1rem;
  }
  
  .trust-point h3 {
    font-size: 1.75rem;
    color: #1e40af;
    margin-bottom: 1rem;
    font-weight: 700;
  }
  
  .trust-point p {
    color: #4b5563;
    font-size: 1.125rem;
    line-height: 1.7;
  }
  
  /* Final CTA */
  .final-cta {
    background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
    color: white;
    padding: 5rem 0;
    text-align: center;
  }
  
  .cta-content h2 {
    font-size: clamp(2rem, 5vw, 3rem);
    margin-bottom: 1rem;
    font-weight: 800;
  }
  
  .cta-content p {
    font-size: 1.25rem;
    margin-bottom: 2rem;
    opacity: 0.95;
  }
  
  .cta-small {
    font-size: 1rem !important;
    margin-top: 1.5rem !important;
    opacity: 0.85 !important;
  }
  
  /* Footer */
  .footer {
    background: #1f2937;
    color: #d1d5db;
    padding: 3rem 0 2rem;
  }
  
  .footer-content {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 3rem;
    margin-bottom: 3rem;
  }
  
  .footer-brand h3 {
    color: white;
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }
  
  .footer-links h4 {
    color: white;
    margin-bottom: 1rem;
    font-size: 1.125rem;
  }
  
  .footer-states {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }
  
  .footer-states a {
    color: #9ca3af;
    text-decoration: none;
    transition: color 0.3s;
  }
  
  .footer-states a:hover {
    color: #3b82f6;
  }
  
  .footer-bottom {
    border-top: 1px solid #374151;
    padding-top: 2rem;
    text-align: center;
  }
  
  .footer-emergency {
    margin-top: 1rem;
    font-size: 1.125rem;
  }
  
  .footer-emergency a {
    color: #10b981;
    text-decoration: none;
    font-weight: 700;
  }
  
  /* Animations */
  @keyframes slideDown {
    from {
      transform: translateY(-100%);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
  
  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
  
  @keyframes slide-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  @keyframes pulse {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
  }
  
  @keyframes pulse-dot {
    0%, 100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.5;
      transform: scale(1.2);
    }
  }
  
  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }
  
  @keyframes bounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-5px);
    }
  }
  
  .fade-in {
    animation: fade-in 0.8s ease-out;
  }
  
  .fade-in-delay {
    animation: fade-in 1s ease-out 0.3s both;
  }
  
  .slide-up {
    animation: slide-up 0.8s ease-out;
  }
  
  .slide-up-delay {
    animation: slide-up 0.8s ease-out 0.2s both;
  }
  
  .pulse {
    animation: pulse 2s infinite;
  }
  
  .bounce {
    animation: bounce 2s infinite;
  }
  
  /* Mobile Responsive */
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }
    
    .hero-subtitle {
      font-size: 1.25rem;
    }
    
    .hero-cta {
      font-size: 1.25rem;
      padding: 1.25rem 2rem;
    }
    
    .states-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    
    .trust-bar-content {
      flex-direction: column;
      gap: 1rem;
    }
    
    .trust-divider {
      display: none;
    }
    
    .popular-grid {
      grid-template-columns: 1fr;
    }
  }
</style>